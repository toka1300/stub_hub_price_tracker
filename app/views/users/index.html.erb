<% provide(:title, 'All users') %>

<section class="container users">
    <h1>All Users</h1>
    <% @users.each do |user|%>
        <li>
            <%= link_to user.name, user %>
        </li>
    <% end %>
</section>