<li>
    <%= link_to user.name, user %>
    <% if current_user.admin? && !current_user?(user) %>
        <%= button_to "Delete", user, method: :delete, data: { turbo: true },
                                            class: "" %>
    <% end %>
</li>